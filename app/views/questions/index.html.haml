%table
  %thead
    %tr
      %th= Question.human_attribute_name(:text)
      %th= Question.human_attribute_name(:owner)
      %th Actions
  %tbody
    - @questions.each do |q|
      %tr
        %td= link_to q.text, questions_path(q)
        %td= q.owner.email
        %td
          - if can? :update, q
            = link_to 'Edit', edit_question_path(q)
            |
          - if can? :destroy, q
            = link_to 'delete', question_path(q), method: :delete, data: { confirm: 'sure?' }
= link_to 'New', new_question_path