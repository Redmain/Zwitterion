%table
  %thead
    %tr
      %th= TestList.human_attribute_name(:name)
      %th= TestList.human_attribute_name(:owner)
      %th Actions
  %tbody
    - @test_lists.each do |t|
      %tr
        %td= link_to t.name, test_lists_path(t)
        %td= t.owner.login
        %td
          - if can? :update, t
            = link_to 'Edit', edit_test_list_path(t)
            |
          - if can? :destroy, t
            = link_to 'delete', test_list_path(t), method: :delete, data: { confirm: 'sure?' }

= link_to 'New', new_test_list_path, class: :'btn btn-success'
= link_to 'Назад', questions_path, class: :'btn btn-success'